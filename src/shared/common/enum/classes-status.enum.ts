export enum CLASSES_STATUS {
    SCHEDULED = 'scheduled',
    IN_PROGRESS = 'in_progress',
    FINISHED = 'finished',
    CANCELED_BY_CLIENT = 'canceled_by_client',
    CANCELED_BY_PROFESSIONAL = 'canceled_by_teacher',
}
